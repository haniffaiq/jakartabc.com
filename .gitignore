# deps
node_modules
.pnpm-store

# build
.next
dist
out
.turbo
build
*.tsbuildinfo

# env
.env
.env.*.local
.env.local
.env.production

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json

# agent worktrees
.worktrees/

# test
coverage
playwright-report
test-results

# payload
# Generated on every boot. Portal's config registers only `users`, so letting
# its copy into the program narrows `CollectionSlug` and breaks typecheck of
# the shared @jakartabc/content collections (it is excluded in tsconfig too).
apps/web/src/payload-types.ts
apps/portal/src/payload-types.ts
apps/web/uploads
